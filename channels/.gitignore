# Downloaded dataset cache -- regenerated by channels/sms/train.py, no need
# to duplicate 466KB of already-public UCI data in the repo.
sms/data/

# Python bytecode cache.
**/__pycache__/
*.pyc

# model.joblib, vectorizer.joblib, manifest.json and metrics.json ARE
# committed (same convention as ml/models/*.joblib) -- they're the real,
# reproducible artifacts this feature's honesty claim depends on.
